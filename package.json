{
  "name": "livora",
  "version": "1.0.0",
  "private": true,
  "description": "Aplicaci\u00f3n de live streaming social estilo Livora Stream: backend Node + app Expo",
  "scripts": {
    "start": "node scripts/start-server.mjs",
    "setup": "npm install --prefix server && npm install --prefix mobile && npm run db:setup",
    "db:setup": "npm run db:push --prefix server && npm run seed --prefix server",
    "dev:server": "npm run dev --prefix server",
    "dev:mobile": "npm run start --prefix mobile",
    "test": "npm test --prefix server && npm test --prefix mobile",
    "typecheck": "npm run typecheck --prefix server && npm run typecheck --prefix mobile",
    "build": "npm run build --prefix server",
    "test:server": "npm test --prefix server",
    "test:mobile": "npm test --prefix mobile"
  }
}
